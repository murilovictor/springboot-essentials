server:
  port: 8080

spring:
  application:
    name: springboot-essentials

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    url:  jdbc:mysql://localhost:3306/db_essentials?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: root
